# Clipwise 第一阶段验收记录

验收日期：2026-06-22

## 自动验证

- `pnpm test`：通过，15 个测试文件、32 个测试用例全部成功
- `pnpm test:e2e`：通过，2 条 Chromium 关键路径全部成功
- `pnpm lint`：通过
- `pnpm build`：通过
- `git diff --check`：通过

## 浏览器验证

- 上传页 1440×900：通过
- 项目页 1024×768：通过，候选栏宽度约 358px
- 项目页 1280×720：通过
- 项目页 1440×900：通过
- 三档桌面尺寸均无横向溢出：通过
- 小于 900px 显示桌面端 Chrome / Edge 提示：通过
- 候选选择与预览状态分离：通过
- 文案和字幕编辑：通过
- 未预览导出提醒：通过
- 原视频重新关联入口：通过
- 推荐排序和时间排序状态可识别：通过
- 页面控制台无错误和警告：通过
- 上传页拖拽 MP4、拖入高亮和无效格式提示：通过

## 第一阶段边界

当前使用模拟 Provider 和演示项目数据。Groq ASR、DeepSeek 评分与标题生成、数据库任务队列、SSE 进度、FFmpeg.wasm 音频提取和真实文件导出将在后续阶段接通。
